# builder-stack — Agent Instructions

This is a **read-only reference and template library**, not a project being
built.

**One rule:** Do not build features here. When asked to use this repo, read
the relevant doc in `docs/`, `templates/`, or `setup/` and apply it in the
*target* project — never modify or scaffold anything inside builder-stack
itself.
